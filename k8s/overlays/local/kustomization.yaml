apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

patches:
  - path: nginx-nodeport.patch.yaml

images:
  - name: flask-app
    newName: sre-demo-web
    newTag: local
